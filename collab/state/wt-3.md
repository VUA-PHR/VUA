---
worktree: wt-3
branch: slot/wt-3
role: 桌面
baseline_commit: 2454a71
updated: 2026-09-08
---
## 当前焦点
**W15 第二轮复验=交互通过+两项必修已交付(2fa260d,待回流+用户确认后关门)**:
①触发时机文案改素材导入时+诚实未接线标注(自动生成本身未接线,跨域需求已路由);
②开关名术语「生成 VPM 包替代」四语同步。008 接线切片=M5 首批(未开窗)。
## 自基线交付(2454a71 合并 main 后,一提交)
- 2fa260d **W15 二轮修正**:①核实结论——生成的唯一触发是手动条目级命令
  (warehouse.generateVpm);导入管线(warehouse_import)与生产宿主均无生成挂点,
  生产侧亦未消费 VPM 副本——开关承诺的「导入时自动生成」未接线;文案改导入时
  语义+行 1 加未接线标注(生成仍需在仓储条目手动发起);②开关名术语「生成 VPM
  包替代」四语同步(en/ja/ko+行 2 依赖文案+注释)。桌面 check 全链绿(47 文件,
  2026-09-08 本机)。
- **跨域需求路由 [→核心][→数据]**:素材导入自动生成——条目落成(warehouse_import
  完成落库)时,若 composed global default=generate_vpm 且守卫允许(有原始件、无
  生成副本),自动提交生成任务(仍走 GenerateVpmTaskSpec,任务化+审计不变);挂点
  位置与编排语义归核心/数据裁决,桌面仅消费任务面呈现;另一关联缺口:生产侧消费
  VPM 副本替代原始(装配素材选择按生效模式)同样未接线,请一并评估排期。
## 阻塞
- W15 关门=用户确认第二轮修正(两修正项已交付);
- 导入时自动生成的 provider/AMF 侧挂点=跨域需求,已路由(见留言),M5 与 008 接线
  一并排期为宜。
## 留言
- [→操作者→用户] W15 第二轮两修正项已交付(触发时机文案+未接线标注;「生成 VPM
  包替代」术语),请确认后关门;
- [→核心][→数据] **跨域需求(导入时自动生成)**:核实结论与需求如上(自基线交付节);
  这是产品语义需求不是缺陷——当前生成纯手动,开关只写偏好。请在 M5 排期时与
  008 路径 a 接线切片(也是生成任务发起时机的桌面侧消费)合并设计;
- [→集成] 二轮修正批(2fa260d)请随轮带入;W15 门=用户确认后关门。
