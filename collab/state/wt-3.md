---
worktree: wt-3
branch: slot/wt-3
role: 桌面
baseline_commit: b007dd2
updated: 2026-09-08
---
## 当前焦点
**用户回传 W15 确认完成＋9 条新发现——本轮只做登记/意图确认/处置草案(用户指令:
不动手改文件)**:桌面负责 1/2/3/7/8/9 五条,逐条理解与歧义见下;无法确定项已入
BOARD「待用户裁决」[需用户]。未改任何实现文件。
## 自基线交付(43ecd6d 合并 main 后,一提交,全部 collab)
- **9 条新发现登记**①意图确认②处置草案③BOARD [需用户] 提问(细节见留言区,
  本节为索引):
  - 1(A4 行 2 自动取消):理解=行 1 关闭时行 2 偏好与状态同步复位;歧义=持久化
    边界与已发生删除不溯;处置=小改(delete-originals-flag 联动);
  - 2(A7 产物模式与条目动作门控):理解=行 1 关→条目无产物模式编辑无动作;
    行 1 开→仅「生成 VPM 包替代」按钮;删除按钮任何情况不出现(删除只由导入
    时规则触发);歧义=条目级模式编辑存废+008 接线语义从「桌面逐条目」改为
    「导入时规则触发」=014/010 语义修订(跨域);处置=条目抽屉撤下+014 修订
    路由核心/环境;
  - 3(素材导入统一页+Web 浏览+审阅子智能体):理解=M6 增设统一导入页(云端
    下载[Web 浏览能力缺口]/本地导入);歧义=Web 浏览定义(BOOTH 浏览 vs 购买
    流)+审阅子智能体本进程不可调用;处置=M6 增设提案草案+审阅安排请操作者;
  - 7(B6 表单否定):理解=文本输入→按钮「选择目录」→系统资源管理器→识别呈现
    →「迁移」/「仅查看」双按钮;歧义=「迁移」与「导入为 VUA 管理的副本」术语
    统一+识别依赖 013 读面路由(未落地);处置=T-C 交互重构(013/014 后);
  - 8(B7~B9 无测试):理解=确认链三段无自动化测试;处置=补状态机/映射纯函数
    与交互测试(桌面域内,可即做);
  - 9(C1 服务端状态不可辨+开发模式设想):理解=取消 DEV 场景条,实验页内
    「开发模式」开关+per-Provider 独立 fixture/live 切换;歧义=架构级(per-port
    装配重构)与 DEV-only 边界;处置=M6/M7 基建提案(路由集成/核心)。
## 阻塞
- 9 条新发现的意图确认与裁决=用户/跨域(BOARD [需用户] 已登记);
- W15 关门=用户确认(已通过——9 条为增量发现,不阻塞已确认部分);
- W24 读面消费=三视图映射规格;检测段接线=013 裁决。
## 留言
- [→操作者→用户][→核心][→环境] **用户回传 9 条新发现登记与意图确认(桌面负责
  1/2/3/7/8/9;逐条理解/歧义/处置草案如下;无法确定项已入 BOARD [需用户])**:

  **1(A4 行 2 自动取消)** 意图:行 1「生成 VPM 包替代」关闭时,行 2「生成后删除
  原始素材文件」应自动复位为关(现仅置灰,偏好仍记忆)。歧义:①复位是否清持久
  偏好(再开行 1 时行 2=关)——桌面理解=是;②行 2 开启期间已发生的删除不溯及
  (已完成删除不可逆)——桌面理解=是。处置草案:行 1 off 时联动
  saveDeleteOriginalsAfterGenerate(false)+确认链文案同步;小改,确认后即做。

  **2(A7 产物模式与条目动作门控)** 意图:行 1 关→条目抽屉无产物模式编辑、无
  条目动作;行 1 开→使用原始 UnityPackage 的素材出现「生成 VPM 包替代」按钮;
  「删除原始素材」按钮任何情况不在本地仓库出现——删除只由导入时按行 2 偏好
  触发。歧义/影响:①条目级模式编辑(radio 三选)是门控还是整体撤下;②删除触发
  从「桌面检测生成副本→逐条目删除」(008 路径 a 现接线)改为「导入时规则触发」
  =008/010/014 语义修订,provider/编排侧跨域;③与 014 R4(半成品不隐式续传)
  的衔接。处置草案:抽屉撤下删除动作入口+产物编辑门控(桌面域内);删除触发
  迁移至导入编排=014 修订提案(路由核心/环境),桌面删除偏好与开关联动改为
  导入表单内选项(行 2 语义并入导入页)。

  **3(素材导入统一页)** 意图:增设统一素材导入页=云端下载+本地导入两段;云端
  段缺 Web 浏览功能;要求调用审阅子智能体审需求,M6 增设阶段。歧义:①「Web
  浏览功能」定义=BOOTH 目录浏览(已有只读轨)还是内嵌浏览器购买流(远程内容
  隔离面已有基建,购买流未规格化);②审阅子智能体本进程无此工具,需操作者
  安排或走集成审阅。处置草案:M6 增设提案(统一导入页=云端轨+本地导入整合,
  复用 W18 对话框与 010 挂点;Web 浏览能力评估另节);审阅安排请操作者明示。

  **7(B6 表单否定→选择目录+迁移/仅查看)** 意图:源路径文本输入框撤销,改为
  「选择目录」按钮→系统资源管理器→选中项目识别后呈现于 VUA→出现「迁移」/
  「仅查看」双按钮。歧义:①「迁移」与已冻结的「导入为 VUA 管理的副本」术语
  统一(同一语义?);②识别呈现依赖 013 读面路由(listProjects/inspectProject,
  未实现);③「仅查看」=013 检视只读面。处置草案:T-C 交互重构(选择→识别→
  双按钮),依赖 013 裁决+路由实现;未路由前双按钮诚实 unavailable。

  **8(B7~B9 无测试)** 意图:确认链三段(plan 面板/apply 回执/unavailable 反馈)
  无自动化测试。歧义:无。处置草案:桌面域内补测——确认链状态机转换(form→
  plan→apply→receipt/rejected)+guard 文案映射+三态 narrow 单测;render 级
  测试项目无先例,以状态机纯函数抽取实现可测性。确认后即做(桌面域内)。

  **9(C1 服务端状态不可辨+开发模式设想)** 意图:C1 核对项指出 fixture 数据与
  服务端数据在 UI 上不可辨别;设想=取消 DEV 场景条,实验页内增「开发模式」开关,
  开启后可单独切换各 Provider 状态(fixture/live per-port),替代按套装切换。
  歧义:①架构级——gateway 装配需 per-port 工厂参数化(现按场景套装装配);
  ②开发模式边界(DEV-only 门控?生产构建如何呈现);③与 DevScenarioBar 的
  关系(替代/共存);④「演示数据」徽标纪律(原则①)在 per-port 模式下的呈现。
  处置草案:M6/M7 基建提案(gateway 装配重构+开发模式页承载+徽标纪律设计),
  路由集成/核心;BOARD 已登记待用户裁决(架构方向与边界)。

- (历史留言消化:mock/W17/008——均已处理。)
