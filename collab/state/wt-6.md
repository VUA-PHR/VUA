## 当前焦点
**EAC 窗口 B 段细节已表态（对产线草案的定稿回复）**：B1 无前置；B2 拆 B2a（签名
核验原语真机断言——对已签名系统进程，可执行）/B2b（完整 EAC 流程——需窗口内有
easyanticheat.exe 进程，若产线 A 段流程不启动 VRChat 客户端则如实记录未覆盖留待
真实残留场景）；B3 确认可执行（对磁盘 EasyAntiCheat 安装文件取证，不依赖进程
运行）；顺序同意产线排列＋进程出现即时插入条款。窗口顺序草案可定稿；窗口义务
（E1–E4 并入 B1–B3）已列；等集成开窗通知。
## 自基线交付（2b97795 后，一提交，全 collab/）
- 合并 main 最新两轮（0e6c93e：产线窗口草案专节＋wt-main 落账）；
- **确认：环境全部 EAC 批次已入 main**（R1a c07ad47 验收 437/0；R2/R3 e08b287
  验收 442/0；R3 签名核验 667346e/de11df1 已入库；窗口前置清单 c0e7dcd 已入库）；
- **B 段细节表态（产线草案定稿输入）**：
  - **B1**（只读探测）：无前置——探测如实报告当前状态（含「无 EAC 进程」情形），
    零干扰确认；
  - **B2**（再核验）拆两层：
    - **B2a 可执行**：签名核验原语真机断言——对窗口机器上的已签名系统进程
      （explorer.exe 或任一系统二进制路径）跑 eac_verify 签名核验，断言
      Verified（验证 WinVerifyTrust 绑定真机正确性）＋eac-probe 输出复核；
    - **B2b 需被测进程**：完整 R3 流程对真实 easyanticheat.exe——依赖窗口内有
      VRChat 启动/运行时段（产线 A 段流程为 Unity 内链路，不含 VRChat 客户端
      启动）；若窗口内无该进程，**如实记录「本窗未覆盖」留待真实残留场景**，
      不造合成进程替代（R8：合成夹具只进 CI，真机证据不造）；
  - **B3**（首批清单证据采集）：确认可执行——取证对象为**磁盘上的
    EasyAntiCheat 安装文件**（VRChat 部署的安装目录，进程不在运行也可取证：
    路径＋WinVerifyTrust 签名＋发布者证据），不依赖进程运行；前提=窗口机器
    装有 VRChat/EAC（W25 真机即用户 VRChat 机器，应满足）；
  - **顺序**：同意产线排列（B1→A1→A2→A3→B2→B3）；补充条款：**A 段执行期间
    若 easyanticheat.exe 进程出现（任意时刻），环境可即时插入 B2b**（核验原语
    执行 <1min，不打断产线）；
  - **证据目录**：环境 `_local_eac/` 确认；归档清单（测试名/输出/文件路径/
    签名状态）在环境状态文件声明；
  - **B 段工具就绪**：eac_probe/eac_verify 已全部在 main（R1a 437/0、R2/R3
    442/0 验收），真机执行=跑 #[ignore] 测试（--ignored），无额外代码准备；
- 产线草案定稿确认随本表态生效（B 段细节已表态，草案可定稿）。
## 阻塞
- 无阻塞。等集成开窗通知（前置①③齐后）；窗口执行义务按「窗口义务」段；
  EAC R1b 终止原语＋九态任务面整合为窗口后切片。
## 窗口义务（定稿顺序＝B1→A1→A2→A3→B2a→[B2b 若进程出现]→B3→E4 归档）
- B1/B2a/B2b/B3 工具均只读；窗口内零终止零清单生效写入（第 1/2 点）；
- 失败如实记录不重试（第 6 点）；证据措辞禁写「已证明安全」（第 7 点）；
- 归档：`_local_eac/`（probe 输出/核验报告/取证记录）＋状态文件声明清单；
- 条目起草随后续批附证据引用（第 8 点）。
## 下次合并意图
本批（B 段表态＋状态，全 collab/）随轮带入免测。此前批（667346e/de11df1 R3
签名核验＋c0e7dcd 窗口清单）请集成一并验收确认。
## 留言
- [→产线] **B 段细节表态（草案定稿输入）**：①B1 无前置；②B2 拆 B2a/B2b——
  B2a（签名原语真机断言，对已签名系统进程）可执行；B2b（完整 EAC 流程）需
  easyanticheat.exe 进程，A 段流程不含 VRChat 客户端启动，若窗口内无该进程
  如实记录未覆盖留待真实残留，不造合成进程；③B3 可执行（对磁盘安装文件取证，
  不依赖进程）；④补充条款：A 段期间 easyanticheat.exe 出现任意时刻环境可即时
  插入 B2b（<1min）；⑤证据目录 `_local_eac/` 确认。草案可定稿；
- [→集成] 窗口顺序草案 B 段已表态（定稿）；开窗前置环境侧=代码全入树 ✓＋
  B 段工具就绪（跑 --ignored 测试，无额外准备）；开窗通知请同步环境（测试
  执行 <15min）；M5 关门验收时环境证据包=`_local_eac/`（gitignore）＋状态
  文件声明清单；
- [→桌面] EAC 呈现归属待派发维持已知悉（无新动作）。
，一提交，全 collab/）
- 合并 main 最新（2b97795，含产线同窗方案批与 wt-3 EAC 表态批）；
- **EAC 窗口前置清单与执行顺序对齐**（对 wt-4 同窗多验证方案的回复，随状态文件
  走，集成在 M5 关门验收时核对证据）：
  - **006 八点语义逐条核对**：
    - 第 1 点（只读侦测为默认能力）：窗口四件套全只读——**窗口内零
      TerminateProcess、零清单生效写入**（终止原语未实现未授权，取证只读
      API）；产线窗口内如执行其他动作请自行确认与本条无冲突；
    - 第 2 点（清单空=未核验/不可用）：窗口是**证据采集窗口**，不是清单生效
      窗口——采集证据（签名者/证书主题/完整路径/哈希/核验状态）只进证据包
      与下一批条目起草，窗口内不落任何「生效清单」位置（v0.1 数据面无生效
      消费方）；
    - 第 3 点（终止末级逐次确认）：窗口内无终止执行，无确认链需求；未来终止
      切片的真机验证另立窗口另行确认，不在本次范围；
    - 第 4 点（四要素任一无法核验即拒绝）：取证按四要素基线记录（名称/PID
      快照/完整路径/WinVerifyTrust 状态＋签名者）——R3 核验原语真机执行本身
      即第 4 点证据；
    - 第 5 点（VRChat 运行=拒绝终止）：探测需覆盖两态——a) VRChat 运行中
      （session_active=true→close_game_first）b) 退出后（residual→
      allowlist_unavailable）；**依赖窗口内有 VRChat 启动/运行时段**（见待确认
      项 1）；
    - 第 6 点（失败不重试）：窗口内任何步骤失败=如实记录 failed 进证据包，
      不重试刷绿，失败项留下窗；
    - 第 7 点（确认≠安全声明）：证据包措辞只写「核验通过/失败」＋证据引用，
      禁写「已证明安全」——归档模板环境出；
    - 第 8 点（清单变化进发行说明）：证据采集后的条目起草随下一环境批（附
      证据引用），发行说明标记由集成在 M6 发行核对；
  - **执行顺序（E1–E4，供产线草案整合）**：
    - **E1**（先行，无前置，<1min）：eac-probe 真机探测（当前机器状态如实
      快照——不假设有无 EAC 残留）；
    - **E2**（需 VRChat 运行中时段，<5min）：会话活跃探测＋R3 再核验
      （对真实 easyanticheat.exe，断言 WinVerifyTrust=Verified）＋四要素取证；
    - **E3**（VRChat 退出后，<1min）：残留状态探测（若 EAC 进程残留则再核验）；
    - **E4**（收尾，<5min）：证据包归档（`_local_eac/` gitignore 照 W1 惯例＋
      状态文件引用）；
    - 环境总占用 <15min；与产线 M5 顺序关系：E2 嵌在产线运行 VRChat 的时段
      （若有），E1/E3/E4 独立可插；
  - **待产线确认三项**：①窗口内是否有 VRChat 客户端启动环节（决定 E2 可行性；
    若无且可协助启动一次 VRChat 请列入顺序）；②窗口开始时机器状态不作假设
    （E1 如实报告）；③证据归档目录约定（环境拟 `_local_eac/`，照 W1 惯例）；
- 006 内联窗口前置清单同步落账。
## 阻塞
- 无阻塞。等产线草案整合（三项确认）→集成开窗确认；窗口执行义务见「窗口义务」。
## 窗口义务（开窗确认后执行）
- E1–E4 按序执行＋证据包归档＋状态文件引用；失败不重试（第 6 点）；
- 窗口内零终止零清单生效写入（第 1/2 点）；
- 证据包措辞纪律（第 7 点）；条目起草随后续批附证据引用（第 8 点）。
## 下次合并意图
本批（状态＋006 落账，全 collab/）随轮带入免测。此前两批（226dd41/667346e+de11df1）
请集成一并验收（667346e/de11df1 待首验）。
## 留言
- [→产线] 同窗方案收到并同意，**前置清单与执行顺序 E1–E4 已随状态文件走**
  （八点语义逐条核对在案）。三个待确认项：①窗口内 VRChat 客户端启动环节有无
  （E2 依赖）；②机器状态不作假设（E1 如实报告）；③证据目录 `_local_eac/`。
  环境总占用 <15min，E2 嵌你们运行 VRChat 的时段即可；草案整合后随轮走，
  排期与开窗确认归集成；
- [→集成] EAC 窗口前置清单已随状态文件走（用户裁决执行件）；开窗确认时请核对
  两点：环境侧前置仅依赖「窗口内有 VRChat 运行时段」（若产线流程无此环节，请
  裁量是否批准窗口内启动一次 VRChat）＋证据包归档位置（`_local_eac/`
  gitignore）；M5 关门验收时环境证据包路径见状态文件「窗口义务」；
- [→桌面] EAC 呈现归属待派发已知悉（F6 不呈现检测卡）；eac-probe v0.1 形状与
  unavailable 口径维持不变，等派发。
