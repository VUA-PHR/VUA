---
worktree: wt-4
branch: slot/wt-4
role: 产线
baseline_commit: ce4a1e1
updated: 2026-09-09
---
## 当前焦点
**W25 合并窗口已获用户批准（产线＋环境同窗）**；产线牵头起草窗口内执行顺序
草案（见下方「W25 窗口执行顺序草案」节），已路由环境对齐——环境对 B 段细节
（eac-probe 被测对象准备等）表态后定稿，随状态文件走，集成在 M5 关门验收时
核对证据。窗口开跑仍等集成确认前置①③齐备后通知。承担 #7 瞬败样本观察义务。
## 自基线交付
- **W21 全部批次已交付并验收**：v2 冻结批（1a9cdf6）＋C# 第一刀（06802b9）＋
  Rust 前置件（8fcff01）＋C# 内核接线（aa2a9da）＋Rust 物化切片（9195fbb，
  W25 前置②备案）＋信封桥接（1438305）＋C# 执行内核接线（aa2a9da）——执行
  链全链连通（核心 job.execute 16a2dc5 消费产线 API 5 处确认）。
- **W25 窗口执行顺序草案**（本轮，用户裁决「同意合并窗口」落账）——见下方
  专节；[→环境] 征求对齐表态；[→操作者→用户] 窗口清单就绪待前置。
- 本轮合并 main（wt-3/wt-6 状态批＋环境 EAC R2/R3 批通知）追平。
- W1（I-1 真 Unity 矩阵）16/16 已验收合并（M3 关门）；amf-unity 1.0.1 批已合并。
## 阻塞
- 无产线侧阻塞。
- W25 等前置①收口声明（核心 v0.2 冻结收口）③（W22 实现切片 provider 记录
  面写入面）；窗口开跑等集成确认前置齐后通知。
## 下次合并意图
本批（窗口草案＋状态，仅 collab/ 增量）随轮带入免测。
## W25 窗口执行顺序草案（产线牵头，2026-09-09——环境对齐后定稿）

**窗口目标**：一次开窗完成产线 M5 全量验证＋环境 EAC 验证四件套；证据归档照
W1 惯例（本地目录＋状态文件声明清单），集成 M5 关门验收核对产线证据。

**开窗前置（缺一不开）**：
- 产线侧：W21 代码全部入树 ✓；W25 前置①（v0.2 冻结 ✓）③（W22 实现切片
  provider 记录面写入面）落地；合法素材环境变量与素材备份就绪（W1 遗留，
  已核实）；冒烟用合法素材（Avatar＋一件衣装）在位；
- 环境侧：EAC R1a/R2/R3 代码入树（R1a ✓，R2/R3 待集成验收）；eac-probe 被测
  对象准备清单（环境提供）；
- 共同：Unity 2022.3.22f1 真机窗口（操作者批准）；串行执行约定（真机 Unity
  单实例，两树验证串行不并行）。

**执行顺序（B 只读探测先行→产线全量→B 再核验收尾）**：

| 序 | 阶段 | 主导 | 内容 | 证据 |
| --- | --- | --- | --- | --- |
| B1 | 环境·只读探测 | 环境 | eac-probe 真机探测（R1a，只读，不改变环境状态） | probe 输出归档 |
| A1 | 产线·EditMode | 产线 | C# EditMode 全套件（含 exclude_object VRCMetaObject 组件断言——实例化＋excluded 字段值断言） | 测试输出归档 |
| A2 | 产线·冒烟路径 | 产线 | 合法素材冒烟端到端：素材备份→导入→Local Resolution→plan.approve→job.execute（Bridge v2）→Build Record→record 读面复核 | 冒烟 log＋Build Record 文档归档 |
| A3 | 产线·构建对比 | 产线 | VRCMetaObject.excluded 构建排除效果对比（excluded on/off 两次构建产物差异） | 构建产物差异记录归档 |
| B2 | 环境·再核验 | 环境 | R2/R3 真机再核验（#[ignore] 用例；在 A 段已改变的环境状态下执行核验原语——正是 EAC 的真实场景） | 核验报告归档 |
| B3 | 环境·证据采集 | 环境 | 首批允许清单证据采集（归档照 W1 惯例） | 允许清单证据归档 |

**顺序理由**：B1 只读先行（零干扰）；产线 A 段居中（导入/构建改变项目与进程
状态）；B2/B3 在被改变后的环境状态中执行——EAC 核验原语的真实场景正是「环境
已发生变化后的核验」，顺序即测试价值。

**窗口内配合约定**：
- 串行执行（真机 Unity 单实例）；两树切换时各自确认工作区归位；
- 任一阶段失败不阻断后续阶段（各自证据独立），失败阶段照诚实纪律记录失败；
- 窗口内发现的缺陷照常走 #7 协议（完整输出回传）；
- 证据目录：产线 `_local_w25/`、环境自定（W1 惯例本地 gitignore）；
  归档清单在各自状态文件声明，集成 M5 关门时核对产线证据。

**产线 A 段前置依赖声明**：A2 冒烟依赖前置③（W22 provider 记录面写入面）与
核心 v0.2 冻结收口；若窗口通知时③未落地，A2 降级为「无 Build Record 的冒烟
路径」（诚实降级声明），A1/A3/B 段不受影响。

## 阻塞
- 无产线侧阻塞。
- W25 等前置①收口声明＋③（W22 实现切片）；窗口开跑等集成确认前置齐后通知；
  环境对草案 B 段细节的对齐表态。
## 下次合并意图
本批（窗口草案＋状态，仅 collab/ 增量）随轮带入免测。
## 留言
- [→环境] **窗口执行顺序草案请对齐**（上方专节）：B1 只读先行／产线 A 段居中
  ／B2/B3 在被改变后环境中收尾的顺序是否符合 eac-probe 与核验原语的测试需要；
  B 段被测对象准备清单（被测进程、允许清单证据采集对象）请环境随表态给出，
  产线在窗口内配合执行顺序。草案定稿后随两树状态文件走，集成 M5 关门核对。
- [→集成] 用户裁决「同意合并窗口」（产线＋环境同窗）已落账；窗口草案产线牵头
  已出，环境对齐后随状态文件走；窗口开跑等前置①③齐后由集成通知。
- [→操作者→用户] 窗口草案已按裁决落账（串行执行＋证据归档惯例＋失败不阻断
  后续）；开窗仍等前置①③＋集成通知。
- 备忘（维持）：#7 样本协议——遇套件瞬败保留完整 panic 输出回传 [→核心]；
  无瞬败不专门加压空跑。
- （历史留言已消化：#7 抖动数据、W1 脚手架事实、amf-unity 批、009/011/012
  互审批、v2 草案/冻结批/012 互审/executor prelude/信封扩展/exclude 形态/
  物化切片验收/信封桥接——均已闭环。）
