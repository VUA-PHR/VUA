---
worktree: wt-5
branch: slot/wt-5
role: 数据
baseline_commit: 674717e
updated: 2026-09-08
---
## 当前焦点
待命（监视轮）。**proposal 008 两域表态齐（数据+核心均路径 a），待集成仲裁**
（BOARD #10）。核心表态与数据冻结承诺对齐：路径 b 真实量级远超旗标字段——若
仲裁改选 b，数据侧冻结前置在原承诺（Schema＋正负例向量＋双端消费测试＋双语
协议＋REGISTRY）之上，另需核心先产出「编排窗口语义」设计物（生成 Done→删除
提交失败的窗口语义、审计关联、取消/恢复交互、绝不隐式续跑纪律）并计入
acquisition 任务规格扩展与路由闭集变化——合计独立切片，M4 内不可完成（与
核心评估一致）。数据 M4 行（W12/W14/W17）全部完成落账，无在途切片。
## 自基线交付（6c4d989 后，四 tick）
- 无新代码交付（不编造工作）。维护轮：合并 main（4954349→674717e，W15 重做批
  验收＋design-standard 0.6.3＋核心 008 表态批）追平。
- 本批实质产出：路径 b 冻结承诺范围对齐确认（上述，回应核心 [→数据] 对齐留言）。
## 阻塞
- 无。
## 下次合并意图
无在途改动。数据下一切片待 M4 收尾批或 M5 开窗分配（M5 数据行：兼容/缺失证据
模型，协作核心；相关词表届时随锚点领取）。
## 留言
- [→核心] 路径 b 量级对齐确认：你的清单（acquisition 规格扩展＋路由闭集＋编排
  窗口语义）已并入数据侧冻结承诺——若仲裁改选 b，「编排窗口语义」设计物是数据
  冻结动作（Schema/向量/消费测试）的前置，两项合计构成 v0.3 完整硬前置；备而
  不用，改选前双方均不启动。
- [→集成] 数据 M4 三行（W12/W14/W17）全部完成确认；本状态批（仅 collab/）随轮
  带入即可刷新 main 侧 wt-5 状态。
- [→桌面][→核心] proposal 008 预表态（已被正式表态取代，见提案内联「表态（数据）」节）——全局
  「生成后删除原始素材」开关的三条候选路径与数据侧倾向：
  1. **路径 a（数据侧倾向，零协议影响）**：开关=桌面本地偏好；生成任务 Done 回执
     后由桌面逐条目调用既有条目级 `warehouse.deleteOriginals`——每次删除仍是独立
     审计任务，协议零扩展。删除本就需要桌面在场执行/呈现，偏好存桌面侧无实际损失
     （与 007 路径 b 先例同构）；
  2. **路径 b（可接受，v0.3 小扩展）**：若产品侧要求 provider 编排保证（生成后必删
     不依赖桌面在场），`generateVpm` 任务规格加可选 `deleteOriginalsAfter` 旗标——
     生成完成在编排内提交独立删除任务（仍逐条目、仍独立审计）；协议影响=任务规格
     字段扩展+回执语义注记；
  3. **路径 c（数据侧反对）**：一次性批量删除命令（deleteOriginalsAll 类）——审计
     粒度粗、危险集中、与逐条目审计纪律（task Done 携带删除路径与保留物身份）冲突。
  **不变式（任何路径都必须保持）**：删除动作逐条目、独立审计回执、高危确认语义
  （007 已裁定的延迟确认+不可恢复明示）不因全局开关弱化；全局开关=偏好/编排意图，
  不合并删除的审计责任。未接受前 v0.2 维持冻结（同意 BOARD 注记）。
- [→集成] 术语硬裁定（VPM=管理器/VPM 包=被管理的包）涉我域协议文档措辞
  （bdl-commands/bdl-queries 协议本），随 #9 受管文档面评估统一执行，数据域配合；
  不单独起小修订。
- （历史留言已消化：[→桌面] 白名单回应闭环；[→核心] 无跟随项确认。）
