# 014 「导入为 VUA 管理的副本」写路径提案（M6 T-A 延伸）

> proposal: 014
> title: 导入为 VUA 管理的副本（唯一写路径的任务化语义：确认链/守卫/审计/恢复）
> status: 已接受（语义冻结；集成仲裁 2026-09-09：独立 project-ops 词表行＋九态任务＋守卫 provider 侧＋确认链桌面形态采纳；实现＝环境 project-manager 域切片，两命令面冻结后桌面接线）
> author: 环境/wt-6
> date: 2026-09-09

## 背景

- 用户裁决 U3（product-boundary 1.2.0）：VUA 对 ALCOM/VCC 管理的原项目只读；唯一写
  路径是用户显式选择的「导入为 VUA 管理的副本」。1.2.0 已钉死五项规格：新项目路径＋
  新项目身份；明示预计磁盘占用；不复制可再生目录与旧任务状态；导入后重新 Inspect
  （不继承原项目的确认/快照）；保留来源关系。
- 用户裁决（2026-09-09 操作者派发）：M6 T-A 提前开工，但任务包明确「提案前只做只读
  能力检测与 Rust 侧实现」。写路径不在只读面内，按提案 013 R5（写命令各自需要独立
  提案）立本提案。
- 桌面 T-C（F6 项目兼容页）已交付诚实占位并留言：检测读面与副本导入执行落地后请
  知会桌面接线——本提案是桌面接线的语义前置。

## 提案（裁决项，逐项待表态）

- **R1 语义（1.2.0 五项规格照录，不扩展）**：导入产生新项目目录（用户选择的目标父
  目录＋项目名）；复制范围＝原项目的 Unity 工程内容；**排除**可再生目录
  （`Library/`、`Temp/`、`Logs/`、`obj/`、`Builds/` 等）与 `.vua/`（旧任务状态、锁、
  标记一律不随行）；完成后对新项目执行完整 Inspect；新项目 `.vua/` 内记录来源关系
  （源路径＋源管理器关联＋导入时间戳），便于返回。
- **R2 确认链**：计划（含来源/目标路径、磁盘占用预估——按复制范围实际统计，非猜测、
  排除清单明示）→ 用户显式确认 → 执行 → 复检（新项目 Inspect 结果回执）。确认走
  桌面 UI 经版本化应用契约；命令面归属与词表路由核心（与提案 013 同框架，作为其
  写命令附录或独立命令另立词表行，由核心裁决）。
- **R3 守卫（执行前逐项核验，任一不符即拒绝并降级为诊断）**：目标路径已存在＝拒绝；
  目标位于源项目目录内部＝拒绝；源路径必须是检测面（project-inspection）识别的
  在册项目＝否则拒绝；磁盘余量低于预估＋安全余量＝拒绝；导入过程中获取**新项目**
  变更锁（原项目只读，不取锁不写标记）。
- **R4 审计与恢复**：导入是可恢复任务（任务化、收据、失败如实失败）；中断/失败后
  **不隐式续传**——残留的新项目目录半成品由复检标记 `inspect_required`，等用户显式
  决定（重试＝清理后重来，或放弃＝用户手动删除）；任务记录排除清单与实际复制的
  顶层条目清单（审计面）。
- **R5 非目标**：不修改原项目的任何文件；不回写 ALCOM/VCC 的注册表/数据库/设置
  （新项目登记为 VUA 管理是 VUA 侧事实，不通知外部管理器）；不提供批量导入
  （一次一项目）；不做跨盘符特殊优化（按普通复制语义，慢如实慢）。
- **R6 裁决流程**：核心（任务面/路由/词表）与桌面（交互形态——与 T-C 四项交互形状
  确认请求合并处理）表态 → 集成仲裁 → 语义冻结 → 环境实现（Rust 侧 project-manager
  域 + 测试）→ wire 接线随 013 词表。裁决前环境不实现写路径，桌面保持占位。

## 关联

- 依赖语义权威：product-boundary 1.2.0（U3）；
- 命令面框架：proposal 013（R1 归属、R3 信封、R4 诚实缺位同样适用）；
- 检测面依赖：`schemas/project-inspection/v0.1`（源项目识别与守卫输入）；
- 桌面对接：T-C F6 页占位（wt-3 留言 2026-09-09）。

## 内联讨论线程

### 实现落账（环境，2026-09-09）

按仲裁实现切片已交付本树（226dd41），请求集成验收：

- **词表冻结件** `schemas/project-ops/v0.1/`：command.schema（`project.import-copy`
  单命令闭集，plan/apply 两 phase；apply 强制 `confirmedPlanDigest`）＋
  result.schema（plan/receipt/rejected 三态；**桌面字段请求已落字段**——
  `estimatedBytes` 逐字节实测、`excludedEntries`、`targetPath`；守卫拒绝码闭集
  七项：target_exists / target_inside_source / source_not_registered /
  source_invalid / insufficient_disk_space / plan_drift / **execution_failed**
  〔执行期失败如实失败——锁/复制/簿记失败非守卫拒绝，经核心裁决第 2/3 条精神
  分型，见下〕）＋正例向量 4 件＋负例向量 3 件；
- **Rust 实现** `crates/project-manager/src/import_copy.rs`：
  `plan_import_copy`（守卫＋实测复制范围＋plan digest）→
  `apply_import_copy`（双摘要漂移拒绝→排除复制→新 Unity 身份
  productName→`.vua/source.json` 来源关系〔含 taskCorrelation，W23 同构〕→
  复检新项目）；
- **纪律落点**：原项目零写入零取锁（守卫核验与复制全程只读源）；新项目锁仅
  覆盖复制窗口；复制失败不清理半成品（inspect_required 语义交任务面）；拒绝码
  `execution_failed` 的说明：核心裁决 2/3 将锁失败与复制失败归入任务内类型化
  失败而非五守卫拒绝——七项闭集=五守卫＋plan_drift（双摘要）＋execution_failed
  （执行期失败），如仲裁认为应并入既有项请指示修订；
- **证据**（2026-09-09 本机）：workspace 全量 0 失败（新增 5 项消费测试：全流程
  含排除/身份/来源链接/原项目未动断言、plan_drift 拒绝、类型化守卫负例、向量过
  冻结 Schema 含负例拒绝）；clippy --workspace --all-targets -D warnings 零告警。


## 表态（核心，2026-09-09——任务面/路由/词表视角）

**总体：提案成立。** R1–R5 与 1.2.0 五项规格、008「绝不隐式续跑」纪律、013 检测
面前置全部对齐。逐项核心意见：

1. **命令面归属与词表路由（R2 请核心裁决项）**：裁为**独立命令词表行**——
   `schemas/project-ops/v0.1/`（project. 前缀，写命令 `project.import-copy`；
   与 013 的 project-inspection 读面 `schemas/project-inspection/v0.1/` 平行、
   读/写分线）。理由：素材域词表（bdl-commands）不混项目域命令；project. 前缀
   与 013 检测读面同域聚拢，能力发现一致；确认链走应用契约（版本化九态任务）。
   冻结硬前置照惯例：命令 Schema＋正负例向量＋至少一端消费测试。
2. **任务面语义（R4）**：导入任务＝九态任务（复用既有任务权威与任务中心；
   commandId 幂等/事件＋revision/可取消全部继承）。**不隐式续传**照 010 纪律：
   失败残留的新项目半成品由复检标 `inspect_required`，重试＝清理后重来（清理
   动作本身用户显式触发，provider 不自动删）。R3 新项目变更锁＝新项目 `.vua/`
   内锁（导入中途防并发写入），原项目全程只读不取锁——与 1.2.0 只读裁决一致。
3. **R3 守卫的实现面归属**：五项守卫全部在导入任务执行前逐项核验（provider
   侧、任务内），任一不符＝类型化失败（拒绝码闭集随命令 Schema 冻结）——守卫
   不在桌面预判（桌面可先做 UX 提示但权威判定在服务端，与 008/012 守卫纪律
   一致）。磁盘余量校验＝按复制范围实际统计源内容（排除清单外）＋安全余量。
4. **证据与审计（与 W23 衔接）**：R4 的审计面（排除清单＋实际复制顶层清单）
   进 Build Record v0.3 惯例的导入收据（或独立导入收据文档，随命令 Schema 冻
   结定形）；`sourceRef.taskCorrelation` 引用与 W23 证据引用同构。
5. **桌面 T-C 接线**：检测读面（013）＋本写命令到位后，F6 页的「导入为副本」
   才是完整闭环——桌面占位维持至两命令面冻结落地。

**待桌面表态**：T-C 交互形态（确认链 UI 与 1.2.0 五项规格的呈现）。
**待集成仲裁**：词表行归属（独立 project-ops vs 013 附录）如桌面/数据无异议
照核心建议；实现排期（环境 Rust 侧 project-manager 域）。


（待核心表态；待集成仲裁。状态维持「提出」，裁决前不实现。）

## 仲裁（集成，2026-09-09）

**裁决：提案成立，语义冻结（R1–R5 照 1.2.0 五项规格不扩展）；词表行归属裁为
独立 project-ops 词表行。** 三域表态核验：核心（提案成立＋五点任务面裁决）与
桌面（接受 R1–R5 全部框架＋UI 形态承诺＋T-C 四项合并回答）均无未决异议；数据
不在表态链（R6 流程未列），其 W23/evidenceIds 交界由核心裁决第 4 条（审计收据
与 W23 证据引用同构）覆盖，无需补表态。

1. **词表行归属（§核心裁决项 1，采纳）**：独立命令词表行
   `schemas/project-ops/v0.1/`（project. 前缀；写命令 `project.import-copy`），
   与 013 的 project-inspection 读面（`schemas/project-inspection/v0.1/`）
   平行、读/写分线——素材域与项目域不混词表，能力发现同域聚拢。
2. **任务面与守卫（核心裁决 2/3 采纳）**：九态任务（commandId 幂等/可取消
   继承）；**不隐式续传**照 010 纪律（半成品复检 `inspect_required`，重试＝
   清理后重来且清理由用户显式触发，provider 不自动删）；R3 新项目变更锁＝新
   项目 `.vua/` 内锁、原项目全程只读不取锁（1.2.0 只读裁决一致）；五项守卫
   全部 provider 侧任务内逐项核验（拒绝码闭集随命令 Schema 冻结；磁盘余量按
   复制范围实际统计＋安全余量）——桌面只做 UX 提示，权威判定在服务端。
3. **审计与 W23 衔接（核心裁决 4 采纳）**：排除清单＋实际复制顶层清单进导入
   收据（随命令 Schema 冻结定形）；`sourceRef.taskCorrelation` 与 W23 证据
   引用同构。
4. **确认链交互（桌面形态采纳）**：入口→目标表单（文件夹对话框＋项目名）→
   计划呈现（来源/目标/磁盘预估实际统计/排除清单明示）→要点确认面板（执行
   授权，要点缺一不呈现即不可确认）→任务化执行＋进度/复检回执走任务中心；
   失败/中断＝inspect_required 类型化状态＋「待决定」条目（重试/放弃均用户
   显式触发）。**桌面字段请求采纳**：磁盘占用预估数值、排除清单、目标路径随
   命令 Schema 冻结落字段。
5. **冻结硬前置照惯例**：命令 Schema（含桌面字段请求）＋正负例向量＋至少一端
   消费测试；实现＝环境 Rust 侧 project-manager 域（语义冻结后按 R9 类似纪律
   开切片）；桌面接线批＝013＋014 两命令面冻结后（与 W18/W19 同批惯例），未
   落地前 F6 入口维持未接线标注（已交付，诚实纪律）。

## 表态（桌面，2026-09-09）

R6 请求的交互形态表态 + T-C 四项交互形状确认的合并回答。桌面接受 R1–R5 全部
框架（与 1.2.0/008 不变式一致）；以下为 UI 侧形态承诺与字段请求。

### A. 确认链交互形态（R2 桌面侧）

- **流**：入口按钮（F6 页「导入为 VUA 管理的副本」，已交付）→ 目标表单
  （目标父目录 = 系统文件夹选择对话框【复用 W18 的 pick 壳能力，单选变体】＋
  项目名文本输入）→ 提交后进入 R2 计划呈现：来源/目标路径、磁盘占用预估
  （实际统计）、排除清单明示 → **要点确认面板**显式确认（形态与 011 §4 批准
  交互同构：要点面板而非延迟按钮——副本导入写的是新路径，非破坏性；但它是
  执行授权，要点缺一不呈现即不可确认）→ 确认后任务化执行，进度/复检回执走
  任务中心；
- **字段请求（R2 呈现的 wire 依赖）**：计划面回执需含磁盘占用预估数值、排除
  清单、目标路径——R2 文本已含，请冻结时落 Schema 字段；
- **失败/中断呈现（R4）**：inspect_required 的半成品在任务中心呈现类型化状态
  +项目兼容页呈现「待决定」条目（重试=清理后重来 / 放弃=用户手动删除，两个动作
  都由用户显式触发，桌面不代删）。

### B. T-C 四项确认的合并回答

1. **页面名与导航归属**：已交付（「项目兼容」页，生产 Tab 包管理旁）——维持；
   副本导入入口在该页（已交付占位），014 实现落地后入口接入确认链；
2. **只读呈现字段粒度（T-B 落地后）**：列表行 = 项目名＋路径＋管理器关联徽标
   （单/双）＋Unity 分类徽标（生产目标/迁移源/其他）＋警告徽标（未完成变更/
   死条目）；详情 = VPM 包声明清单（packageId＋声明/锁定版本）＋SDK 行＋检测
   警告全文——字段集 = 兼容矩阵 1.0.0 检测行逐一对应，具体列布局 W24 UI 批定；
3. **写操作交接形态**：第一切片 = 纯文案指引卡（呈现管理器名与操作类型指引，
   已交付）；不建议外部拉起 ALCOM/VCC 应用（路径/协议未规格化，且与 R5「不回
   写外部管理器」精神一致）；后续如产品要求再另议；
4. **副本导入路径选择**：目标父目录 = 系统文件夹选择对话框（W18 壳能力单选
   变体）；项目名 = 文本输入；五项规格在确认面板逐条呈现（已交付的规格文案
   迁移进面板）。

### C. 依赖与排期意见

- 命令面归属（R2「写命令附录或独立词表行」）归核心裁决——桌面两侧均可实现，
  无偏好；
- 桌面接线批 = 013 词表冻结 + 014 语义冻结后（与 W18/W19 同批惯例），未落地
  前 F6 入口维持未接线标注（已交付）。

（desktop 立场：R1–R5 无修订意见；交互形态如上，供集成仲裁与核心词表裁决。）


## 实现验收（集成，2026-09-09）

**实现批验收合并（226dd41 经合并入 main；集成复跑 cargo workspace 428 通过
0 失败〔56 套件，含新增 production_use_case_vectors 与 import_copy〕＋clippy
-D warnings 零告警）。**

1. **实现范围核验**：`import_copy.rs` 607 行——`plan_import_copy`（守卫＋实测
   复制范围＋plan digest）→ `apply_import_copy`（双摘要漂移拒绝→排除复制→新
   Unity 身份）＋import_copy.rs 342 行契约测试＋schemas/project-ops/v0.1/
   词表冻结件（command/result Schema＋正例 4＋负例 3）。
2. **七项拒绝码闭集仲裁确认**（回应环境实现批请示）：闭集七项
   （target_exists / target_inside_source / source_not_registered /
   source_invalid / insufficient_disk_space / plan_drift / execution_failed）
   **确认采纳，随 project-ops v0.1 冻结**——五守卫对应 1.2.0 规格，plan_drift
   （双摘要漂移）与 execution_failed（执行期失败如实失败，非守卫拒绝）的分型
   符合仲裁第 2 条精神与 008/012 守卫纪律；语义边界清晰，无异议。
3. **桌面字段请求落实核验**：`estimatedBytes`（逐字节实测）、`excludedEntries`、
   `targetPath` 已随词表冻结落字段（桌面确认链呈现依赖就绪）。
4. **后续**：桌面接线批＝013＋014 两命令面均已冻结——桌面接线解锁（F6 入口
   接入确认链）；exclude_object（009 互审）待 W25 真机核验 marker 形态。

## 执行面增强（环境，2026-09-09 深夜——用户裁决 7/9/12 派生）

apply 成功路径在写完 `source.json` 后**追加一步**：对新项目写 VUA 独有标识文件
`.vua/project.json`（裁决 9：副本＝体验上「迁移到了 VUA」→ 副本天然 VUA 原生；
裁决 7：迁移后项目加 VUA 独有标识文件）。词表零变更（receipt/envelope/拒绝码
闭集全部不动，实现内部行为）；原始项目不写标识（只读纪律不变）。标识文件格式、
三态语义与备注原语见 013 v0.2 注记（`vua_identity` 模块；测试
`tests/vua_identity.rs` 5 项＋happy path 标识断言）。
