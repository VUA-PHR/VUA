# 006 EAC 实验性恢复适配器边界裁决稿

> proposal: 006
> title: EAC 实验性恢复适配器边界（裁决稿）
> status: 已接受（用户整体批准 R1–R9，无修订，2026-09-08）
> author: 环境/wt-6
> date: 2026-09-07

## 背景

- 开发计划大纲（M6，v0.8.0）任务行：「EAC 实验性恢复适配器（先出边界裁决稿）」，负责角色
  环境，裁决集成；BOARD U1 [需用户]：**批准边界条款后才有实现**。
- 架构权威（`docs/architecture/integrations-and-overlays_ZH.md` §环境修改与 EAC 恢复）已定硬约束：
  环境工具先只读侦测，返回证据、就绪结论和人工路径；下载/安装/写入/提权/进程动作必须是明确的
  计划步骤，经用户确认并执行后复检；EAC 冲突侦测默认只读；终止 EAC/VRChat 残留进程属默认
  关闭、实验性、高风险的末级选项，仅限允许清单中的用户态残留进程，执行前核验并展示名称、PID、
  路径、签名/发布者和原因，存在活跃游戏会话时拒绝执行。
- 产品边界（`docs/product-boundary_ZH.md`）已定风险表述：终止可能导致游戏中断、EAC 状态异常，
  极端情况下触发平台或账号安全检查；不得停止服务/驱动、修改 EAC/VRChat 文件或规避反作弊。

本裁决稿不改变上述任何权威表述，只把适配器实现前仍开放的点整理为编号裁决项，交集成裁决与
用户批准。**未经用户批准，不写一行实现代码。**

## 提案（裁决项，逐项待批准）

- **R1 能力面**：适配器只含两级能力——(a) 只读 EAC 冲突侦测（证据 + 就绪结论 + 人工路径，
  沿用 env 引擎 presence 模型）；(b) 默认关闭、逐次确认的「终止允许清单内用户态残留进程」。
  其余一律不做（见 R7 非目标）。
- **R2 允许清单治理**：清单条目必须含进程名、预期路径模式、签名/发布者证据、列入原因四要素；
  内置清单初始为空，首批条目只能来自真机核验证据（需真机窗口），每次变更进发行说明。
- **R3 执行前核验**：每次终止前重新核验名称/PID/路径/签名并整屏展示给用户，与清单不符即拒绝
  并降级为诊断；核验失败不算发现，不得猜测补齐。
- **R4 活跃会话拒绝**：检测到 VRChat 进程在运行即视为活跃会话、拒绝终止（保守定义，从宽拒绝）。
- **R5 确认与披露**：全局开关默认关闭；逐次执行前弹出账号风险披露（沿用产品边界原文语义）并
  要求再次确认；确认走桌面 UI 经版本化应用契约完成，终止能力不暴露给网页、Overlay 或插件。
- **R6 可观察与恢复**：每次终止是一个计划步骤：执行前留证据快照、执行后复检进程确已退出并给出
  下一步指引；失败如实报失败，不自动重试，不进入隐式恢复（`inspect_required` 语义）。
- **R7 非目标（硬边界，逐条复述架构禁令）**：不停止服务或内核驱动；不修改 EAC/VRChat 文件或
  配置；不注入/挂钩/隐藏进程；不以任何方式规避反作弊；不批量/定时/无确认终止；不向插件宿主
  开放本能力。
- **R8 测试与证据纪律**：CI 只用合成进程夹具（结构代表、无真实 EAC 交互）；真机证据（窗口、
  产物）齐备前，终止开关不得标记为可用——界面诚实显示「未核验」；验收走 M6 门。
- **R9 裁决流程**：集成对本稿仲裁修订 → 用户按 U1 批准（逐项或整体）→ 环境在 M6 窗口实现；
  用户未批准前本提案保持「提出」，环境不动代码。

## 内联讨论线程

### 仲裁（集成，2026-09-07）

R1–R9 逐项对照权威核对，**无修订，仲裁通过**：

- R1/R7 与 `docs/architecture/integrations-and-overlays_ZH.md` §环境修改与 EAC 恢复的硬约束
  逐条一致（只读默认、末级选项实验性、不碰服务/驱动/文件、不规避反作弊）；
- R3/R4 从严：核验失败不算发现、活跃会话保守从宽拒绝——与「不得猜测」「存在活跃游戏会话时
  拒绝执行」一致；
- R5 确认链路走版本化应用契约、能力不暴露给网页/Overlay/插件——符合依赖方向与插件边界；
- R6 的「不自动重试、不进入隐式恢复（inspect_required）」与诚实纪律第 3 条一致；
- R8 的「真机证据齐备前界面诚实显示未核验」与诚实纪律第 1/5 条一致；CI 合成夹具符合安全
  边界（真实 EAC 交互不进 CI）；
- R2 清单初始为空、条目仅出自真机核验——从严，防止清单自证循环。

状态维持「提出」，待用户按 U1 逐项或整体批准；批准前环境不动代码（R9）。

### 用户批准（2026-09-08，经操作者转达，第三方仲裁复核回传）

**R1–R9 整体批准，无修订**。批准的准确含义按用户八点精确界定执行（部分严于本稿正文，
实现与验收以本节为准）：

1. 默认能力只有只读侦测；
2. 初始允许清单为空——真机证据产生首个合格条目前，终止能力应显示「**未核验/不可用**」，
   而不只是关闭状态（较本稿 R8 的「未核验」更严：不可用）；
3. 终止始终是末级、逐次确认的计划步骤；**全局开关不是永久授权**；
4. 名称、PID、路径或签名任一无法重新核验，均拒绝执行；
5. 检测到 VRChat 运行时拒绝终止；
6. 失败不重试，重启后进入 `inspect_required`；
7. 用户确认只表示接受已披露风险，**不构成「操作已被证明安全」的产品声明**；
8. 允许清单每次变化必须有真机证据并进发行说明。

**实现措辞依据（R8 收紧，裁决稿正文不改）**：R8 正文「界面诚实显示『未核验』」在实现与
验收时按用户第 2 点收紧为「未核验/不可用」——初始允许清单为空期间，终止能力一律呈现为
不可用，而非仅处于关闭状态；用户第 7 点措辞入产品披露文案，禁止把用户确认表述为安全背书。

状态：**已接受（用户整体批准 R1–R9，无修订，2026-09-08）**。M6 窗口实现授权生效；
实现切片开工前先合并 main 最新，按 R9 与上述八点语义执行。

### 实现落账（R1a 只读侦测切片，环境，2026-09-09）

M6 提前授权范围含本 EAC 包（outline 2.0.9 拆行，BOARD M6 行）。按 R9 纪律交付第一
切片（226dd41 后续批 c07ad47），请求集成验收：

- **R1a 只读侦测已实现**：`crates/project-manager/src/eac_probe.rs`（probe_eac）＋
  `schemas/eac-probe/v0.1/probe.schema.json`。进程表只读快照（Toolhelp32 注入源，
  零写入零终止）；EAC/VRChat 进程分类（名称表大小写不敏感，无关进程永不成为
  发现）；R4 活跃会话判定（VRChat 在列=活跃会话）；就绪结论三态＋guidance 码
  （编辑器矩阵纪律）；**八点语义第 2 点落字段**——terminationCapability v0.1
  冻结单态 unavailable（allowlist_empty_no_machine_evidence），比关闭更强；
- **R8 纪律**：CI 合成进程夹具（5 项测试零真实 EAC 交互；枚举失败=类型化错误
  非清洁账单；确定性断言）；真机探测为 #[ignore] 手动测试（输出进证据记录，
  零写入零终止）；
- **边界**：R1b 终止面（确认链/核验/终止原语/允许清单数据面）为独立后续切片；
  路径核验（R3 的 QueryFullProcessImageName）属终止面职责，读面如实报 null；
- **证据**（2026-09-09 本机）：workspace 全量 0 失败＋clippy -D warnings 零告警；
  真机探测待窗口执行（#[ignore]）。

### 实现落账（R2/R3 数据面与核验原语切片，环境，2026-09-09）

第二切片（e08b287），请求集成验收：

- **R2 允许清单数据面**：`schemas/eac-allowlist/v0.1/allowlist.schema.json`
  （清单起始为空——设计初态；条目四要素＋evidenceRef/addedAt/releaseNotes
  出处字段；通配符拒绝）＋ `eac_allowlist.rs` 全校验加载器（**半有效条目=
  加载错误，绝不弱化条目**）；fixtures＝空表＋带文档的示例条目；
- **R3 核验原语**：`eac_verify.rs`（verify_candidate）——名称比对（pid 仍在
  表且名称与条目一致）、可执行路径读取（OpenProcess 仅
  PROCESS_QUERY_LIMITED_INFORMATION＋QueryFullProcessImageNameW，只读查询）
  ＋大小写不敏感前缀模式匹配、签名 v0.1 如实 Unverified（R3：任一要素无法
  核验即拒绝——**设计判定为 Refused**，WinVerifyTrust 绑定随终止切片落地）；
  v0.1 不打开终止权限、不调用 TerminateProcess；
- **证据**（2026-09-09 本机）：workspace 全量 0 失败（新增 5 项测试：fixtures
  校验、类型化加载错误、模式匹配含形近前缀拒绝、核验报告含未核验签名拒绝、
  pid 消失/名称不匹配候选）＋clippy -D warnings 零告警；真机再核验为
  #[ignore] 手动测试。

### 实现落账（R3 签名核验补齐——R3 四要素完成，环境，2026-09-09）

第三刀（667346e），请求集成验收：

- **R3 四要素全部可核验**：签名核验接入真实 WinVerifyTrust
  （GENERIC_VERIFY_V2、无 UI、无吊销检查、STATEACTION VERIFY＋CLOSE 释放）；
  status 0＝Verified，无签名/提供者拒绝/其他失败＝类型化 Unverified（状态码进
  detail）。R3 语义完整达成：名称＋路径＋签名（外加 pid 存活）在终止面前全部
  再核验，任一不通过即拒绝；对已签名的在册二进制 Verified 可达；
- 上一切片的「恒定 Refused」暂态行为被真实判定取代；测试更新（不存在的文件→
  Unverified 带状态码；测试二进制自身→Unverified）＋Windows 测试钩子暴露签名
  判定；
- **证据**（2026-09-09 本机）：workspace 全量 0 失败＋clippy -D warnings 零
  告警；签名通过路径的真机验证待窗口（#[ignore] 手动测试覆盖）。

### 实现落账（R3 签名核验补齐——四要素核验完成，环境，2026-09-09）

第三刀（425ffe3），请求集成验收：

- **签名核验接入真实 WinVerifyTrust**（GENERIC_VERIFY_V2、WTD_UI_NONE、无吊销
  检查、STATEACTION VERIFY＋CLOSE 释放）：status 0＝Verified；无签名
  （TRUST_E_NOSIGNATURE）/提供者拒绝/其他失败＝类型化 Unverified（状态码进
  detail）。**R3 语义完整达成**：名称＋路径＋签名（外加 pid 存活）在终止面前
  全部再核验，任一不通过即拒绝；对嵌入签名的在册二进制 Verified 可达；
- **catalog 签名现象如实文档化**：系统 catalog 签名二进制（如 cmd.exe）在
  GENERIC_VERIFY_V2 文件验证下返回 TRUST_E_NOSIGNATURE——已知 WinVerifyTrust
  行为，类型化 Unverified（R3 从严拒绝）；EAC loader 为嵌入签名，不受影响
  （W25 窗口 B2b 断言点）；
- 上一切片「恒定 Refused」暂态被真实判定取代；测试更新（不存在文件→Unverified
  带状态码；测试二进制自身→Unverified；catalog 签名 cmd.exe→Unverified 注记）
  ＋Windows 测试钩子暴露签名判定；B2b 真机件改为从运行进程的实际镜像路径起草
  条目（R2 证据而非 fixture）；
- **证据**（2026-09-09 本机）：workspace 全量 0 失败＋clippy -D warnings 零
  告警；Verified 路径的真机断言待 W25 窗口（B2b，对 EasyAntiCheat.exe）。

（已接受；R1a/R2 已验收 437/0、442/0；R3 签名核验待验收；R1b 终止原语＋确认
链整合为下一切片。）

### 合并窗口前置清单（环境，2026-09-09——用户批准同窗多验证，产线牵头草案）

按八点语义逐条核对 EAC 验证四件套（探测/再核验/首批清单证据/Verified 断言）的
窗口内环境状态要求，全文随 wt-6 状态文件走。要点：

- 窗口内零终止、零清单生效写入（第 1/2 点）：窗口是证据采集窗口，不是清单生效
  窗口；
- 无确认链需求（第 3 点）：终止原语未实现未授权，未来终止切片真机验证另立窗口；
- 探测需覆盖两态（第 5 点）：VRChat 运行中（session_active→close_game_first）与
  退出后（residual→allowlist_unavailable）——依赖窗口内有 VRChat 运行时段；
- 取证即第 4 点证据：四要素基线（名称/PID/完整路径/WinVerifyTrust 状态＋签名者）
  由 R3 核验原语真机执行产出；
- 失败如实记录不重试（第 6 点）；证据措辞禁写「已证明安全」（第 7 点）；条目起草
  随后续批附证据引用、发行说明标记归集成（第 8 点）；
- 执行顺序 E1（状态快照）→E2（会话活跃探测＋再核验＋取证）→E3（残留态探测）→
  E4（证据包归档 `_local_eac/`），环境总占用 <15min。

（同窗窗口的排期与开窗确认归集成；多树共享批准权在操作者。）
