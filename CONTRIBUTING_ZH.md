# 为 VUA 贡献

[English](CONTRIBUTING_EN.md) | [简体中文](CONTRIBUTING_ZH.md)

> 文档版本：1.1.0
> 状态：已接受
> 权威语言：简体中文（EN 为镜像，同步至 1.1.0）

VUA 当前处于 pre-alpha。项目欢迎贡献，但产品边界和版本化契约优先于快速扩张实现范围。

## 开始之前

1. 阅读[开发文档导航](docs/README_ZH.md)，并按任务进入对应文档。
2. 大型功能、新依赖、协议、架构或产品范围变更应先新建或参与 Issue。小型文档修正和范围明确的
   缺陷修复不要求事先讨论。
3. 在聚焦的分支中修改，不要在同一 Pull Request 中混入无关清理。

## 贡献应包含什么

- 跨模块行为先修改所属契约，再修改实现。
- 优先提交可运行的小型纵向切片，不提交大范围空壳。
- 新行为需要测试；正确性或恢复缺陷需要回归测试。
- 长任务在契约允许时应可观察、可取消、可恢复并可安全重试。
- 修改现行开发文档时同步更新 `_EN.md` 和 `_ZH.md`。协议 Schema、源码、生成文件和正式许可证
  文本保持单一来源。
- 新依赖应明确用途、许可证、所有者与移除路径。

## 安全、隐私与素材

不得提交付费素材、含付费内容的 `.unitypackage`、用户 Unity 项目、Cookie、访问令牌、订单数据、
生产数据库、私人日志、凭据或 `.env` 文件。仓库与云端 CI 测试必须使用与真实输入结构一致、但不含
真实商品或用户内容的合成数据。

本地只读兼容性测试可以访问公开 BOOTH 页面，但不得把网页响应、截图或商品元数据固化为仓库夹具。
开发者可以在本地使用自己合法取得或购买的素材运行 Unity 集成与冒烟测试；素材、项目、测试配置、
日志和输出不得上传到仓库或云端 CI 制品。所有测试均须遵守第三方的购买、付费、年龄、认证和访问
控制。

安全漏洞请按 [SECURITY.md](SECURITY.md) 使用 [GitHub 私密报告](https://github.com/VUA-PHR/VUA/security/advisories/new)。普通故障、建议和使用问题请提交 [Issue](https://github.com/VUA-PHR/VUA/issues/new/choose)。

## 开源发布与验收证据

- 入库并随源码发布：源码、测试、Schema 与测试向量、受管文档和发行说明。
- 验收结论入库：各 M 门的验收结论写入发行说明，并附机器可读验收清单；原始日志、截图与
  运行产物不入库，按 `_local_*` 约定保留在本地。
- CI 是公开可复核验收：GitHub Actions 运行 cargo test/clippy、pnpm check 与 Schema 向量
  校验，徽章即公开验收状态；真实 Unity 矩阵保持本地执行，结论写入发行说明。
- remote 于 M3（v0.5.0）本地验收通过当日建立：推送全历史、回填既有门标签、发布 v0.5.0
  发行说明与验收清单；此后每个 M 门一律 tag + Release。

## 集成路径

所有改动（包括内部协作簿记）均通过 Pull Request 进入 main；必需检查通过并解决审查对话后合并。当前人员配置不强制额外独立审批，同一账号下的多个 Agent 不算独立审批。工作区操作者遵循[主干保护规则](collab/PROTECTED_MAIN.md)。

## 贡献的许可证

本仓库采用 [Apache License 2.0](LICENSE)。依据该许可证第 5 节，除非贡献者明确另行声明，主动
提交并计划纳入 VUA 的贡献均以 Apache-2.0 提供，不附加额外条款。贡献者仍保留其作品的版权。

VUA 当前不要求签署 Contributor License Agreement，也不强制 `Signed-off-by`。只有经过明确记录的
治理决策才能改变这一政策。

VUA 名称和视觉标识的使用另受[商标说明](TRADEMARKS_ZH.md)约束。

## 文档变更日志

- 1.1.0（2026-09-22）：补充可用的私密报告和 Issue 入口；集成改为 PR 路径。

- 1.0.0（2026-09-06）：纳入版本管理；新增"开源发布与验收证据"节（入库范围、验收结论与
  机器可读清单、原始证据本地保留、CI 公开复核、真实 Unity 矩阵本地执行、remote 于 M3 本地
  验收当日建立）。
