# VUA Unity Bridge

该 Unity 2022 Editor Package 是 VUA Orchestrator 与 Unity 项目之间的确定性执行边界。当前版本
实现项目检查、素材识别、Modular Avatar 衣装装配、菜单开关、装配验证和本地性能估算。

协议由仓库根目录的 `schemas/unity-bridge/v1/` 和 `docs/protocols/unity-bridge-v1.md` 定义。
包内测试只使用运行时创建的合成对象，不包含付费 Avatar 或衣装素材。
